# Vercel deployment requires PostgreSQL instead of SQLite.
# Create a Vercel Postgres database in your project storage tab and paste the connection string below.
DATABASE_URL="postgresql://user:password@host/database"
JWT_SECRET="growwise_super_secret_key_2026_change_in_production"
PORT=5000
